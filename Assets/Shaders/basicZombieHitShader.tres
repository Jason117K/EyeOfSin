[gd_resource type="Shader" format=3 uid="uid://dmuekynfvi6pn"]

[resource]
code = "shader_type canvas_item;
uniform vec4 target_color : source_color;
uniform vec4 replace_color : source_color;
uniform vec4 glow_color : source_color;
uniform vec4 modulate_factor : source_color = vec4(9.0, 9.0, 9.0, 1.0);
uniform float tolerance : hint_range(0.0, 1.0);
uniform float glow_tolerance : hint_range(0.0, 1.0) = 0.5;

void fragment() {
    vec4 tex_color = texture(TEXTURE, UV);

    float dist = distance(tex_color.rgb, target_color.rgb);

    if (dist < tolerance) {
        tex_color.rgb = replace_color.rgb;
    }

    COLOR = tex_color;
}"
